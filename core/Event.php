<?php

namespace Core\Events;

/**
 * A base class for all event objects.
 * Events are simple, immutable data containers that represent a significant
 * action that has occurred in the application.
 */
abstract class Event
{
    // This can be extended later to add common event functionality if needed.
}
