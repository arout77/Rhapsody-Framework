{% extends "layouts/main.twig" %}

{% block content %}
    <h2 class="text-3xl font-bold text-gray-800 dark:text-gray-200 mb-6">Dashboard</h2>
    <p class="text-gray-600 dark:text-gray-400">Welcome, {{ auth.user.name }}!</p>
    <p class="text-gray-600 dark:text-gray-400 mt-2">You have successfully logged in. This page is only visible to authenticated users.</p>
{% endblock %}